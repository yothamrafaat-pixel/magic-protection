# Database Backup & Restore Guide

## نظرة عامة
هذا المجلد يحتوي على سكريبتات لأخذ نسخ احتياطية واستعادة قاعدة البيانات بسهولة.

---

## 🔧 الإعداد الأول (تشغيل مرة واحدة فقط)

اتبع هذه الخطوات لإنشاء الأيقونات على سطح المكتب:

### الخطوة 1: فتح PowerShell
1. اضغط على `Windows + R`
2. اكتب `powershell`
3. اضغط `Enter`

### الخطوة 2: تشغيل سكريبت الإعداد
انسخ والصق هذا الأمر:
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\dell\magic-protection\setup-backup-shortcuts.ps1"
```

### النتيجة:
ستجد على سطح المكتب أيقونتين جديدتين:
- 📦 **BackupDatabase.lnk** - لأخذ نسخة احتياطية
- ↩️ **RestoreDatabase.lnk** - لاستعادة البيانات

---

## 📦 أخذ نسخة احتياطية

### الطريقة 1: النقر على الأيقونة
انقر نقراً مزدوجاً على **BackupDatabase** على سطح المكتب

### الطريقة 2: من PowerShell
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\dell\magic-protection\backup-database.ps1"
```

### ماذا يحدث:
✅ يتم حذف النسخة القديمة تلقائياً
✅ يتم أخذ نسخة جديدة من قاعدة البيانات
✅ يتم حفظها في: `C:\Users\dell\magic-protection\backups\magic-protection.db.backup`
✅ يتم عرض معلومات النسخة (الوقت والحجم)

---

## ↩️ استعادة البيانات من نسخة احتياطية

### الطريقة 1: النقر على الأيقونة
انقر نقراً مزدوجاً على **RestoreDatabase** على سطح المكتب

### الطريقة 2: من PowerShell
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\dell\magic-protection\restore-database.ps1"
```

### خطوات الاستعادة:
1. سيطلب منك تأكيد العملية
2. اكتب `YES` للمتابعة (يجب أن تكون بأحرف كبيرة)
3. ستستعاد البيانات من النسخة الاحتياطية

⚠️ **تحذير:** هذا سيستبدل قاعدة البيانات الحالية بالنسخة الاحتياطية

---

## 📂 مواقع الملفات

| الملف | الموقع |
|------|--------|
| قاعدة البيانات الحالية | `C:\Users\dell\magic-protection\prisma\magic-protection.db` |
| النسخة الاحتياطية | `C:\Users\dell\magic-protection\backups\magic-protection.db.backup` |
| سكريبت الأخذ | `C:\Users\dell\magic-protection\backup-database.ps1` |
| سكريبت الاستعادة | `C:\Users\dell\magic-protection\restore-database.ps1` |

---

## 💡 نصائح مهمة

1. **اخذ النسخ بانتظام**: خذ نسخة احتياطية بعد إضافة بيانات جديدة مهمة
2. **نسخة واحدة فقط**: يتم الاحتفاظ بنسخة احتياطية واحدة فقط، والنسخة الجديدة تحل محل القديمة
3. **تأكيد الاستعادة**: عند الاستعادة، يجب تأكيد بكتابة `YES`
4. **إغلاق التطبيق**: إغلق متصفح الويب قبل الاستعادة لتجنب مشاكل القفل

---

## ❓ حل المشاكل

### المشكلة: "ملف النسخة الاحتياطية غير موجود"
**الحل:** تأكد من تشغيل سكريبت الأخذ مرة واحدة على الأقل

### المشكلة: "لا يمكن الوصول للملف"
**الحل:** 
- أغلق متصفح Edge إذا كان مفتوحاً (يحجز الملفات)
- أغلق التطبيق وحاول مرة أخرى

### المشكلة: "خطأ في التنفيذ"
**الحل:** تأكد من تشغيل PowerShell كمسؤول

---

## 📋 قائمة التحقق

- [ ] قمت بتشغيل `setup-backup-shortcuts.ps1`
- [ ] ظهرت الأيقونتان على سطح المكتب
- [ ] قمت بأخذ نسخة احتياطية أولية
- [ ] تحقق من وجود الملف في مجلد `backups`

---

آخر تحديث: 2026-06-12
